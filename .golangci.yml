version: "2"

linters:
  default: standard
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - gocritic
    - misspell

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck

formatters:
  enable:
    - gofmt
